import React, { ReactElement } from 'react';
import AppRoutes from './AppRoutes';

function App(): ReactElement {
  return (
    <div className="App">
      <AppRoutes />
    </div>
  );
}

export default App;
